server:
  port: 8000
spring:
  application:
    name: wiki

  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wiki?characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456

# mybatis
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml

# 打印
logging:
  level:
    com.yuemaz.wiki: trace

